tasks:
  - name: eslint
    init: npm install eslint --save-dev
  - name: servidor
    init: npm install
    command: node index.js

ports:
  - port: 3000
    onOpen: open-browser

vscode:
  extensions:
    - dbaeumer.vscode-eslint
